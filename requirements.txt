textual>=0.70.0
JACK-Client>=0.5.4
numpy>=1.24.0
soundfile>=0.12.1

# Transcription (optional for audio-to-text feature)
faster-whisper>=1.1.0

# GPU Acceleration (optional - for faster transcription with NVIDIA GPU)
# Uncomment to enable CUDA support:
# torch>=2.0.0
# Or install separately with: pip install torch --index-url https://download.pytorch.org/whl/cu121
